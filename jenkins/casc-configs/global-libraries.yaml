unclassified:
  globalLibraries:
    libraries:
      - defaultVersion: "master"
        implicit: true
        name: "common-lib"
        retriever:
          modernSCM:
            scm:
              git:
                id: "common-lib"
                remote: "/var/jenkins_home/scripts-repo"
