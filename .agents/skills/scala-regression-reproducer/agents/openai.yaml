interface:
  display_name: "Scala Regression Reproducer"
  short_description: "Reduce community-build failures to scala-cli repros"
  default_prompt: "Use $scala-regression-reproducer to confirm a Scala regression and reduce it to a self-contained scala-cli reproducer."
